This directory can host optional runtime files (fallback configuration, fonts, etc.).
No LilyPond binary is embedded here. The embedded binary will be provided via the
binary artifact bundle in Binaries/ by Release Engineering for actual releases.

